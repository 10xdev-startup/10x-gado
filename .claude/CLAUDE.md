# gstack

Use the `/browse` skill from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools.

Available gstack skills:
- `/office-hours` — structured thinking / async collaboration session
- `/plan-ceo-review` — review a plan from a CEO perspective
- `/plan-eng-review` — review a plan from an engineering perspective
- `/plan-design-review` — review a plan from a design perspective
- `/design-consultation` — design consultation and feedback
- `/review` — code review
- `/ship` — ship a feature end-to-end
- `/land-and-deploy` — land and deploy changes
- `/canary` — canary deploy
- `/benchmark` — benchmark performance
- `/browse` — web browsing (use this for ALL web browsing)
- `/qa` — QA a feature
- `/qa-only` — QA without code changes
- `/design-review` — review design artifacts
- `/setup-browser-cookies` — set up browser cookies for authenticated browsing
- `/setup-deploy` — set up deployment pipeline
- `/retro` — run a retrospective
- `/investigate` — investigate a bug or issue
- `/document-release` — document a release
- `/codex` — use Codex for a task
- `/cso` — chief of staff operations
- `/autoplan` — automatically plan a feature
- `/careful` — proceed carefully with extra caution
- `/freeze` — freeze changes
- `/guard` — guard against regressions
- `/unfreeze` — unfreeze changes
- `/gstack-upgrade` — upgrade gstack to the latest version

If gstack skills aren't working, run `cd .claude/skills/gstack && ./setup` to build the binary and register skills.
